# QuizMaster Pro

Flutter + Drift offline quiz app aligned to the provided plan.

## After creating the project
1. Run `flutter pub get`
2. Run `flutter pub run build_runner build --delete-conflicting-outputs`
3. Replace `assets/questions.json` with the full question bank when ready.

The code bundle includes the full app structure, providers, screens, widgets, and database layer.
